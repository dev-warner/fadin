language: node_js
node_js:
  - 8

jobs:
  include:
    - stage: release
      node_js: lts/*
      deploy:
        provider: script
        skip_cleanup: true
        script:
          - npx semantic-release
cache:
  directories:
    - node_modules
before_install:
  - npm update
install:
  - npm install
script:
  - npm run coveralls
  - npm run build
deploy:
  provider: surge
  project: ./docs/
  domain: fadin.docs.surge.sh